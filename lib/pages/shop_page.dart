import 'package:flutter/material.dart';

class ShopPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ShopPage',style: TextStyle(color: Colors.white))),
      body: Center(
        child: Text('Shop Page'),
      ),
    );
  }

}